@model IEnumerable<Models.Thong_Bao>

@{
    ViewData["Title"] = "Danh sách thông báo";
}
<div class="card shadow mb-4">
    <div class="card-body">
        <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
            <div class="table-responsive">
                <table class="table dataTable" id="normalTable">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Ten)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Ngay)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Hien_Thi)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            var arg = new Tennis_Web.Controllers.MethodController.ConfirmViewModal()
                            {
                                BtnMsg = "Xóa",
                                Message = "xóa thông báo " + item.Ten,
                                ActionName = "DeleteAnnouncement",
                                ControllerName = "File",
                                Id = item.Id.ToString()
                            };
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Ten)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Ngay)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Hien_Thi)
                                </td>
                                <td>
                                    <a asp-action="UpdateAnnouncement" asp-route-id="@item.Id" class="btn btn-info">Sửa</a>
                                    <button type="button" class="btn btn-danger" data-toggle="ajax-modal" data-target="#confirmModal" data-url="@Url.Action("ConfirmModal", "Method", arg)">Xóa</button>
                                </td>
                            </tr>

                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Ten)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Ngay)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Hien_Thi)
                            </th>
                            <th></th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
<div class="form-group">
    <a asp-action="UpdateAnnouncement" class="btn btn-primary">Thêm mới</a>
</div>
<!-- Modal placeholder -->
<div id="modal-placeholder"></div>